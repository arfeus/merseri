<% provide(:title, 'Ideas') %>
<div class="container">
	<%= render 'mers/search' %>
	<br  />
	<br  />
  <% @mers.each do |mer| %>
	<% @mer = mer %>
	<%= render 'show_mer' %>			  
  <%= link_to 'Show', mer_path(mer) %>
  <% end %>
</div>
